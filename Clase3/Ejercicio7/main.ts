// exportaciones de shapes.ts

import { circleArea, squareArea } from "./shapes";

const radio = 5;
const lado= 15;

console.log(`Area del circulo (radio ${radio}):`, circleArea(radio));
console.log(`Area del cuadrado (lado ${lado}):`, squareArea(lado));
