//importamos 

import {Geometry} from './geometry';

const radio = 10;
const lado = 15;

console.log(`Este es el area del circulo (radio ${radio}):`, Geometry.areaOfCircle(radio));
console.log(`Este es el area del cuadrado (lado ${lado}):`, Geometry.areaOfSquare(lado));